{
  "name": "gha-aws-ecs-run-task",
  "version": "1.0.0",
  "description": "Runs AWS ECS tasks via GitHub Actions",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./dist && ncc build --source-map ./src/index.ts",
    "prepack": "npm run build",
    "test": ":"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.7.0",
    "@moebius/aws-ecs-task-runner": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^12.20.49",
    "@vercel/ncc": "^0.33.1",
    "aws-sdk": "^2.1120.0",
    "npm-run-all": "^4.1.5",
    "ts-node": "^10.7.0",
    "tsconfig-paths": "^3.14.1",
    "typescript": "^4.6.3"
  }
}
